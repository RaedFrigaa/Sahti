"use client";
import { useRouter } from "next/navigation";
import { useState } from "react";
import { Logo, PrimaryButton } from "@/components/ui";
export default function Login(){const [role,setRole]=useState("cabinet");const r=useRouter();return <main className="grid min-h-screen place-items-center bg-brand-soft p-4"><form onSubmit={e=>{e.preventDefault();r.push(role==="admin"?"/admin":"/cabinet")}} className="w-full max-w-md rounded-2xl bg-white p-7 shadow-card"><Logo/><h1 className="mt-8 text-3xl font-bold">Espace professionnel</h1><p className="mt-2">Connexion simulée pour les cabinets et administrateurs.</p><label className="mt-6 block font-bold">Email ou nom d’utilisateur<input required className="mt-1 min-h-11 w-full rounded-xl border border-brand-muted px-3"/></label><label className="mt-4 block font-bold">Mot de passe<input required type="password" className="mt-1 min-h-11 w-full rounded-xl border border-brand-muted px-3"/></label><label className="mt-4 block font-bold">Accès de démonstration<select value={role} onChange={e=>setRole(e.target.value)} className="mt-1 min-h-11 w-full rounded-xl border border-brand-muted px-3"><option value="cabinet">Cabinet</option><option value="admin">Administrateur</option></select></label><PrimaryButton className="mt-6 w-full">Se connecter</PrimaryButton></form></main>}

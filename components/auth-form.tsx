"use client";
import { useState } from "react";
import { PrimaryButton } from "./ui";
export function AuthForm() { const [mode,setMode]=useState<"login"|"signup">("login"); const [code,setCode]=useState(false); const [message,setMessage]=useState(""); function submit(e:React.FormEvent){e.preventDefault();setCode(true)} function verify(e:React.FormEvent){e.preventDefault();setMessage("Connexion simulée réussie ! Vous pouvez maintenant effectuer vos demandes de rendez-vous.")} return <div className="mx-auto max-w-md rounded-2xl bg-white p-6 shadow-card"><div className="mb-6 flex rounded-xl bg-brand-soft p-1"><button onClick={()=>{setMode("login");setCode(false)}} className={`min-h-11 flex-1 rounded-lg font-bold ${mode==="login"?"bg-white":""}`}>Connexion</button><button onClick={()=>{setMode("signup");setCode(false)}} className={`min-h-11 flex-1 rounded-lg font-bold ${mode==="signup"?"bg-white":""}`}>Inscription</button></div>{!code?<form onSubmit={submit} className="space-y-4">{mode==="signup"&&<><label className="block font-bold">Nom<input required className="mt-1 min-h-11 w-full rounded-xl border border-brand-muted px-3"/></label><label className="block font-bold">Prénom<input required className="mt-1 min-h-11 w-full rounded-xl border border-brand-muted px-3"/></label></>}<label className="block font-bold">Numéro de téléphone<input required type="tel" placeholder="0550 00 00 00" className="mt-1 min-h-11 w-full rounded-xl border border-brand-muted px-3"/></label><PrimaryButton className="w-full">Recevoir un code SMS</PrimaryButton></form>:<form onSubmit={verify} className="space-y-4"><p>Un code de vérification simulé a été envoyé à votre numéro.</p><label className="block font-bold">Code SMS<input required pattern="[0-9]{4,6}" title="Saisissez 4 à 6 chiffres" inputMode="numeric" placeholder="1234" className="mt-1 min-h-11 w-full rounded-xl border border-brand-muted px-3"/></label><PrimaryButton className="w-full">Valider le code</PrimaryButton></form>}{message&&<p role="status" className="mt-5 rounded-xl bg-status-confirmed-bg p-4 font-semibold text-status-confirmed">{message}</p>}</div> }
